# Node.js dependencies
node_modules/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
config.env
.env.* # If you have multiple environment files like .env.local, .env.production, etc.

# Directory for test coverage reports
coverage/

# Build output (for example, if using TypeScript or other build tools)
dist/
build/

# OS-specific files
.DS_Store       # macOS Finder metadata
Thumbs.db       # Windows image thumbnail cache

# IDE or editor settings (optional, but good to include if you're using specific IDEs)
.vscode/
.idea/
*.swp           # Vim swap files

# Dependency directories for package managers
.pnpm/
.yarn/

# Temporary files from various tools
*.tmp
*.bak
*.swp

# Package-lock files or lock files from other package managers (if not committing them)
package-lock.json
yarn.lock

# Optional: Ignore any test-specific or CI-specific files (like test output)
*.tgz
*.gz

# Coverage directories from tools like Istanbul/nyc
.nyc_output/

# JetBrains project files (if using IntelliJ/WebStorm)
.idea/

# npm global installs (if used globally in your environment)
npm/
